function calculo(){
    const valor = Number(document.getElementById("valord").value)
    const resp = document.getElementById("resposta")
    if(valor < 1){
        resp.innerText = "Valor Insuficiente"
    }
    if(valor >= 1 && valor < 1.75){
        
    }
}